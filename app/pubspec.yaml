name: jyotish_app
description: Vedic astrology for the German market.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ^3.4.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  flutter_riverpod: ^2.5.0
  go_router: ^14.0.0
  drift: ^2.18.0
  sqlite3_flutter_libs: ^0.5.20
  dio: ^5.4.0
  timezone: ^0.9.4

  # AGPL engine — runs ON DEVICE only. See docs/AGPL-BOUNDARY.md.
  # jyotish_engine:
  #   git:
  #     url: https://github.com/YOUR-GITHUB-USERNAME/jyotish-engine.git
  #     path: packages/dart

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  drift_dev: ^2.18.0
  build_runner: ^2.4.0

flutter:
  uses-material-design: true
  generate: true
